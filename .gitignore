# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Jekyll build output and local gem cache
_site/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
Gemfile.lock

# Pagefind search index (built by GitHub Action; not tracked)
_pagefind/

# Raw video downloads from the interview pipeline (1-3 GB, not committed).
# Whisper transcripts under archive/transcripts/ ARE committed.
videos/

# OCR scratch + raw OCR text dumps (the headered .md versions are kept; raw .txt are not)
ocr_work/
archive/documents/*_OCR.txt

# Node (only if a React/Vite build is ever added)
node_modules/
dist/
.cache/
npm-debug.log*

# Python (only if notebooks/scripts are added)
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Temp
*.tmp

# ──────────────────────────────────────────────────────────────────────────
# Copyrighted material — kept LOCALLY for preservation but NOT published in
# this repo. The website links to the originals instead (her christinecotton.com
# pages for her own works; the original publishers for third-party documents).
# ──────────────────────────────────────────────────────────────────────────
# Her GCP expertise reports, summaries, update, McCullough slides (PDFs)
pdfs/Vaccine-expertise*.pdf
pdfs/ENG-Vaccine-expertise*.pdf
pdfs/ENG-SUMMARY-Expertise*.pdf
pdfs/SUMMARY-EVAL-PFIZER*.pdf
pdfs/Résumé-Exp-*.pdf
pdfs/Summary-Exp-*.pdf
# Her book (offered free, but excluded from the repo per owner's choice)
pdfs/The_Trial_was_almost_Perfect*.pdf
# Extracted text of all the above
archive/documents/Expertise_*.md
archive/documents/Summary_*.md
archive/documents/Update_*.md
archive/documents/Book_*.md
# Third-party source documents (site links to their original publishers)
pdfs/m5351-c4591001-*.pdf
pdfs/CDC-2021-0089-*.pdf
archive/sources/VAERS_*.md

# NOTE: the PDFs that REMAIN tracked (CV, the OPECST presentation, plus any
# site images you add) are still sizeable, so configure Git LFS before the
# first commit:  git lfs install && git lfs track "pdfs/**"   (see TODO.md).

# Local-only backups of her X / Twitter / social posts (kept for preservation
# in case posts or accounts are later removed; not republished per the
# copyright policy in CLAUDE.md). Drop screenshots, .html, .json, or .txt
# captures into archive/social/ and they'll stay out of git.
archive/social/

# Christine Cotton tweet archive — preserve locally, do not republish (copyright policy)
tweets/
